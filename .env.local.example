# ============================================
# Azure Pricing Collection - Local Development Configuration
# ============================================

# Copy this to .env.local and customize for your environment

# ADX Configuration (required)
# Get these values from your deployed Terraform infrastructure:
# - terraform output adx_cluster_uri
# - terraform output adx_database_name
ADX_CLUSTER_URI=https://your-adx-cluster.region.kusto.windows.net
ADX_DATABASE_NAME=pricing-metrics

# Local Development Settings
MAX_PRICING_ITEMS=6000
JOB_TYPE=local-dev
LOG_LEVEL=INFO

# Production-Ready Performance Settings
# Number of retries for 5xx errors and network issues
API_RETRY_ATTEMPTS=3
# Seconds to wait between retries
API_RETRY_DELAY=2.0

# API Filtering (recommended for testing and focused collection)
# Use FILTERS_JSON to filter data at the Azure Pricing API level
# Examples:
# Virtual Machines in East US: FILTERS_JSON='{"filters": {"serviceName": "Virtual Machines", "armRegionName": "eastus"}}'
# Storage services: FILTERS_JSON='{"filters": {"serviceFamily": "Storage"}}'
# Specific product: FILTERS_JSON='{"filters": {"productName": "Virtual Machines"}}'
# FILTERS_JSON='{"filters": {"serviceName": "Virtual Machines", "armRegionName": "eastus"}}'

# Azure Authentication
# When running locally, the application will use:
# 1. Azure CLI authentication (az login) - Recommended
# 2. VS Code authentication
# 3. Environment variables (not recommended for development)

# Container Apps Production Settings (set automatically in Container Apps)
# AZURE_CLIENT_ID=<set-by-terraform>
# AZURE_TENANT_ID=<set-by-terraform>
# AZURE_SUBSCRIPTION_ID=<set-by-terraform>

# Note: Make sure your Azure account has the following permissions:
# - ADX Database Admin role on the target database (for local development)
# - Container Apps jobs use User-assigned Managed Identity with ADX Admin role
